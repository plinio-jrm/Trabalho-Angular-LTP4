import { Injectable } from '@angular/core';
import { BIQUINI_API } from '../app.api';
import { Http } from '@angular/http';

@Injectable()
export class BiquiniService {

  constructor(private http: Http) { }

  
}
